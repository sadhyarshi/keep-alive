name: Keep Alive — HRMS

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HRMS Backend
        run: |
          curl -f https://hrms-dbqd.onrender.com/health
          echo "✅ Server pinged successfully"